# 企业内部 VLM-RAG 项目开展说明

本文档用于说明“基于页面图像的视觉检索增强生成（VLM-RAG）”项目在启动阶段应如何理解、拆解和推进。重点不在代码实现细节，而在项目目标、技术路线、工程边界、评估方式和后续迭代路径。

## 1. 项目背景

企业内部文档通常不是干净的纯文本数据。合同、报表、PPT、费用单据、制度文件、设备手册等材料大量存在图文混排、表格、图表、签章、多栏布局、页眉页脚和批注信息。

传统文本 RAG 通常先经过 OCR 和版面解析，再将解析出的文本切分后进入向量检索链路。这个方案在纯文本文档上可行，但在复杂图文文档上会遇到明显瓶颈：

- OCR 识别错误会直接传导到召回和生成阶段。
- 表格行列关系、图表趋势和 PPT 视觉层级在文本化后容易丢失。
- 合同条款、签章、脚注等版式信息很难通过普通文本 chunk 完整表达。
- 多页文档中，答案往往分散在摘要页、明细页和附录页之间，单页或单 chunk 检索不足以支撑稳定问答。

因此，本项目采用页面图像作为主要检索单元，用视觉语言模型对文本 Query 和页面图像进行统一编码，再基于 Top-K 页面完成多页问答生成。

## 2. 项目目标

项目目标分为业务目标、算法目标和工程目标三类。

### 2.1 业务目标

面向企业内部复杂文档，支持用户用自然语言提问，并返回答案、证据页面和检索分数。典型问题包括：

```text
采购合同的服务期限是什么？
季度经营报表里 Q3 收入是多少？
销售复盘中哪个区域排名第一？
设备 E17 告警代表什么？
工龄 5 到 10 年年假是多少天？
```

系统输出不应只给出答案，还需要保留可追溯证据，方便业务人员核对。

### 2.2 算法目标

算法侧重点包括：

- 将文本 Query 与页面图像编码到统一向量空间。
- 提升复杂图文页面的召回效果。
- 降低 OCR 版面解析错误对最终答案的影响。
- 支持 Top-K 多页证据融合。
- 建立可量化评估指标，而不是只依赖主观案例判断。

核心指标包括：

- `MRR@10`：衡量正确页面在前 10 个结果中的排序质量。
- `Recall@K`：衡量 Top-K 是否召回正确证据页面。
- `EM`：衡量答案是否与标准答案完全一致。
- `Accuracy`：衡量整体回答正确率。
- 单页处理耗时：衡量实际部署成本。

### 2.3 工程目标

工程侧需要形成完整闭环：

```text
数据构建 -> 页面预处理 -> 向量索引 -> 双塔检索 -> 多页生成 -> 指标评估 -> 误差分析
```

项目不应停留在单个模型调用脚本，而应具备配置管理、日志记录、数据划分、索引持久化、基线对比和结果产出能力。

## 3. 核心方案

本项目的基本思路是将“文档页”作为检索粒度，而不是将 OCR 文本切成若干 chunk。

整体链路如下：

```text
企业文档
  |
  v
按页渲染为图像
  |
  v
页面图像编码
  |
  v
页面向量索引
  |
  v
用户 Query 编码
  |
  v
Top-K 页面召回
  |
  v
多页视觉问答
  |
  v
答案 + 证据页面 + 指标
```

该方案的关键点有三个。

第一，检索单元从文本 chunk 变为页面图像。页面级粒度可以保留表格、图表和版式结构，证据定位也更直观。

第二，文本 Query 和页面图像进入同一个向量空间。这样可以通过相似度完成跨模态检索。

第三，生成阶段不简单拼接图片，而是对 Top-K 页面逐页理解，再根据检索分数和页面证据进行融合。这种方式比直接拼接大图更稳定，也更便于控制成本。

## 4. 数据建设思路

数据质量决定该项目的上限。项目初期不要急于堆模型，应该先把数据规范和评估集做扎实。

### 4.1 文档类型

建议优先覆盖六类企业文档：

| 类型 | 特点                       | 典型问题                     |
| ---- | -------------------------- | ---------------------------- |
| 合同 | 条款密集、字段关键、签章多 | 服务期限、付款比例、违约责任 |
| 报表 | 表格和图表混合             | 收入、利润、现金流、周转天数 |
| PPT  | 视觉层级明显               | 区域排名、核心卖点、阶段目标 |
| 单据 | 字段固定                   | 金额、申请人、审批状态       |
| 手册 | 流程和故障码多             | 告警含义、处理步骤           |
| 制度 | 规则和条件分支多           | 年假、调休、远程办公规则     |

### 4.2 样本结构

每条样本至少包含：

```text
query_id
query_text
answer
positive_page_ids
doc_type
```

每个页面至少包含：

```text
page_id
doc_id
doc_type
page_no
image_path
metadata
```

如果项目需要训练检索器，还需要构造正负样本。正样本是能回答问题的页面，负样本可以来自同 batch 其他页面，也可以通过 hard negative mining 选择语义相近但答案不对的页面。

### 4.3 数据处理原则

建议遵循以下原则：

- 按文档维度划分训练集、验证集、测试集，避免同一份文档泄漏到不同集合。
- 保留页面原图，不要只保存 OCR 文本。
- 对敏感信息先脱敏再进入训练和评估流程。
- Query 要覆盖事实型、定位型、比较型和跨页型问题。
- 模糊问题和无法从页面证据回答的问题应过滤掉。

## 5. 模型与算法设计

### 5.1 双塔检索器

双塔检索器包含两个编码入口：

```text
文本塔：Query -> query embedding
视觉塔：Page image -> page embedding
```

两个 embedding 需要处于同一向量空间。线上检索时，只需要编码用户 Query，再与离线构建好的页面向量索引计算相似度。

训练目标推荐使用 InfoNCE：

```text
让 Query 与正确页面更接近
让 Query 与错误页面更远
```

在 batch 内，可以将其他样本的页面作为负例。后续如果数据规模上来，应加入 hard negative，例如同一文档类型、字段相似但答案不同的页面。

### 5.2 隐藏层加权池化

对于 VLM 编码器，不同隐藏层表达的信息不同：

- 浅层更偏视觉纹理和局部结构。
- 中层更偏版式和区域关系。
- 深层更偏语义概念。

隐藏层加权池化的目的，是让页面向量同时保留版式信息和语义信息。权重可以通过验证集搜索，也可以在微调时学习。

### 5.3 多页视觉生成

生成模块输入 Top-K 页面和用户问题。实际落地时可以使用 MiniCPM-V、GPT-4o 或企业内部多模态模型。

不建议初期直接采用“把多页图片拼接成一张大图”的方式。该方法虽然实现简单，但容易带来：

- 分辨率下降。
- 页面内容拥挤。
- 模型注意力分散。
- 证据页定位不清晰。

更稳妥的方式是：

```text
Top-K 页面逐页推理 -> 页面级候选答案 -> 基于检索分数和置信度融合
```

## 6. 基线设计

没有基线，无法证明方案有效。项目初期至少应保留以下对比：

| 基线         | 目的                                       |
| ------------ | ------------------------------------------ |
| OCR-RAG      | 验证页面图像方案是否优于传统文本链路       |
| SigLIP       | 验证普通全局图文向量的效果                 |
| ColPali      | 对比更偏页面版式和 late interaction 的方法 |
| 图像拼接问答 | 验证逐页推理融合是否优于简单拼接           |

评估时需要保证：

- 使用同一测试集。
- 使用同一指标口径。
- 记录每个方法的 Top-K 结果。
- 保留错误案例，方便分析问题来源。

## 7. 项目推进节奏

建议按阶段推进，而不是一开始就接入全部真实模型和复杂基础设施。

### 阶段一：跑通最小闭环

目标是验证方向可行。

范围：

- 20-50 页样本文档。
- 20-100 条问答。
- 页面级检索。
- 简单答案生成。
- 基础指标评估。

产出：

- 最小可运行链路。
- 初始测试集。
- 第一版指标。

### 阶段二：补齐基线

目标是建立对比参照。

范围：

- OCR-RAG。
- 图文向量检索。
- 页面图像问答。
- 图片拼接问答。

产出：

- 基线指标表。
- 主方案与基线差异分析。

### 阶段三：优化检索器

目标是提升正确页面召回。

范围：

- Query 扩充。
- InfoNCE 微调。
- 隐藏层池化权重搜索。
- hard negative 构造。
- Top-K 参数调优。

产出：

- 训练日志。
- 检索指标提升。
- 不同文档类型的效果对比。

### 阶段四：优化生成模块

目标是提升最终答案准确率。

范围：

- Top-K 页面逐页问答。
- 检索分数加权。
- 多候选答案投票。
- 证据页引用。
- 失败兜底策略。

产出：

- 问答准确率。
- 错误案例分析。
- 生成链路耗时。

### 阶段五：工程化落地

目标是让系统具备可维护和可部署能力。

范围：

- 离线页面索引。
- 增量更新。
- 日志和监控。
- API 服务。
- 权限控制。
- 文档脱敏。
- 成本评估。

产出：

- 可部署服务。
- 运维指标。
- 业务验收报告。

## 8. 风险点与处理建议

### 8.1 数据规模不足

样本太少时，指标容易虚高，模型优化也不稳定。

建议：

- 每类文档至少准备几十到几百页。
- 每类问题至少覆盖事实、定位、比较和跨页问题。
- 指标按文档类型分别统计。

### 8.2 OCR 基线过弱

如果 OCR-RAG 基线实现过于粗糙，主方案优势不具备说服力。

建议：

- OCR 基线也要认真实现。
- 保留 OCR 后处理、表格解析、文本切分策略。
- 对比时说明两套方案的处理链路。

### 8.3 生成模型不可控

多模态模型可能出现幻觉或引用错误页面。

建议：

- 强制输出证据页面。
- 答案必须来自 Top-K 页面。
- 对低置信度结果返回“无法确定”。
- 保留候选答案和页面分数。

### 8.4 成本和延迟

VLM 编码和多图问答成本高于纯文本链路。

建议：

- 页面向量离线计算。
- 高频文档缓存。
- 控制 Top-K 数量。
- 对长文档做分层检索。
- 记录单页处理耗时和单次 Query 耗时。

## 9. 验收标准

项目是否达标，建议从以下维度判断。

### 9.1 效果验收

- `MRR@10` 高于 OCR-RAG 基线。
- `Recall@K` 能稳定召回正确证据页面。
- 图表、表格、PPT 页面上的准确率有明显提升。
- 多页问题能够给出合理证据组合。

### 9.2 工程验收

- 数据构建流程可重复。
- 页面索引可持久化。
- 模型参数和路径可配置。
- 训练、评估、推理日志完整。
- 输出结果可追溯到证据页面。

### 9.3 业务验收

- 业务人员能理解答案来源。
- 错误案例能定位原因。
- 系统延迟在可接受范围内。
- 敏感数据处理符合企业要求。

## 10. 后续迭代方向

如果继续推进，可以按优先级考虑：

1. 接入真实 PDF/PPT 页面渲染，替换当前模拟数据。
2. 接入真实 VLM 编码器，例如 MiniCPM-V、SigLIP 或 ColPali。
3. 使用 FAISS、Milvus 或 Elasticsearch 替换本地 JSON 索引。
4. 引入真实多模态问答模型，例如 MiniCPM-V 2.6 或 GPT-4o。
5. 扩充跨页问题、图表问题和表格问题。
6. 建立错误案例库，按文档类型分析失败原因。
7. 增加 API 服务和前端证据页展示。
8. 引入权限、脱敏、审计和增量索引机制。

## 11. 项目启动 Checklist

正式开展项目前，建议确认以下事项：

- 文档类型是否明确。
- 目标用户和典型问题是否明确。
- 是否有可脱敏的样本文档。
- 是否定义页面级元数据结构。
- 是否定义 Query、答案、证据页标注规范。
- 是否准备 OCR-RAG 基线。
- 是否明确 MRR、Recall、EM、Accuracy 的计算口径。
- 是否计划按文档维度划分训练集和测试集。
- 是否考虑页面索引缓存和增量更新。
- 是否记录错误案例和耗时指标。

## 12. 结论

这个项目的核心不是简单调用一个多模态模型，而是围绕企业图文文档建立一套完整的页面级 RAG 方法。

关键判断是：

```text
当文档中的重要信息强依赖页面版式、表格、图表和视觉结构时，
页面图像级检索比纯 OCR 文本检索更有优势。
```

项目初期应优先跑通小规模闭环，建立可靠评估集和基线，再逐步替换真实模型、扩充数据和工程化部署。这样推进风险较低，也更容易证明方案价值。
